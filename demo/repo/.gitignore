# Создаётся рекордером на время прогона, в git не едет: см. secrets/README.md.
secrets/aws-credentials
# Симлинк-побег сценария S4 — тоже создаёт рекордер.
logs/escape.log
# «Зависимость» с postinstall: рекордер ставит её перед прогонами S5 и снимает после.
vendor/
# Продукты демо-сборки.
dist/
.mcp.json
