# E0 — многомерное сканирование (review-scan)

codeTree anchor: `b22e0e3403f5617eeecf45230738961c4d616da5`

Диапазон: `c65f71eafc0a230cb4227e8ec6f49eea21b3b6c6..HEAD` (23 коммита).
Режим: read-only. Прогон тестов и сборка **не** выполнялись — все утверждения ниже
выведены чтением исходников, схемы, снапшота поверхности и истории коммитов.

Итог: **2 Major, 7 Minor, 5 Nit.**

---

## 1. Скан на дефекты + расхождения «комментарий/док ↔ код»

### Major — сырая строка `pattern` всё-таки пересекает границу пакета

`packages/contracts/src/tool.ts:98`
(плюс `packages/contracts/src/types.ts:31-37`, `packages/contracts/src/validate/regex.ts:14-16`,
`docs/10-honest-limitations.md` раздел «ReDoS: чего стоил замер», spec R29)

Заявление записано в трёх местах одинаково: «через границу пакета едет **скомпилированный
матчер**, а не строка `pattern`… получив строку, потребитель вправе вызвать
`new RegExp(pattern)` и вернуть ровно тот вектор, который закрыт на загрузке. У матчера
наружу не выставлены ни `source`, ни `flags`».

Строка выезжает наружу двумя путями, и оба — в этом же диффе:

1. `parseManifest` возвращает `{ ok: true, manifest, matchers }`
   (`src/validate/index.ts:76`). `StringParam.pattern` объявлен **обязательным**
   (`src/manifest.generated.ts:94`, `schema/mcpproxy.schema.json:84`), то есть
   `manifest.tools[r].params[p].pattern` — та самая голая строка, лежащая рядом с матчером.
2. `toTool` копирует её дословно в `inputSchema.properties[p].pattern`
   (`src/tool.ts:98`) — то есть в payload, который уезжает модели через `tools/list` и
   компилируется бэктрекящим движком клиента. Тест это фиксирует как желаемое
   (`src/tool.test.ts:88`).

Конкретность: `src/validate/parse.test.ts` (кейс «не бэктрекит на вложенном
квантификаторе») доказывает, что `^(a+)+$` — **законный** паттерн манифеста: RE2 проходит
его за 0.009 мс и `parseManifest` возвращает `ok: true`. Этот же паттерн затем уезжает
строкой в `Tool.inputSchema` и в `Manifest`. `regex.test.ts` («не выставляет наружу source и
flags») проверяет только форму объекта-матчера и потому зелёный.

Fix (поверхность заморожена, поэтому реалистичен второй вариант):
— либо убрать `pattern` из проекции `toTool` и не отдавать `manifest` целиком, оставив
`matchers` единственным каналом (ломает форму `ParseManifestResult` и `Tool` — до заморозки
это ещё возможно, после нет);
— либо честно переписать абзац в `10-honest-limitations.md` и комментарии
`types.ts:31-37` / `regex.ts:14-16`: RE2 закрывает ReDoS **внутри демона на загрузке**, а
паттерн виден потребителю и через `Manifest`, и через `Tool`, и гарантия на клиента MCP не
распространяется. Сейчас документ, который существует ради честности границ, границу
переоценивает.

### Major — `asRecipeName` слабее, чем `propertyNames` схемы, и `constructor` проходит

`packages/contracts/src/ipc.ts:21-26`

```ts
export const RECIPE_NAME_PATTERN = /^[a-z][a-z0-9_]{0,63}$/;
export function asRecipeName(value: string): RecipeName {
  if (!RECIPE_NAME_PATTERN.test(value)) throw new TypeError(`не имя рецепта: ${value}`);
```

Схема ограничивает имя **двумя** условиями (`schema/mcpproxy.schema.json:18-21`):

```json
"propertyNames": { "pattern": "^[a-z][a-z0-9_]{0,63}$",
                   "not": { "enum": ["constructor", "prototype", "__proto__"] } }
```

`docs/07-contracts.md` прямо объясняет, зачем вторая половина: «Одного паттерна мало:
`constructor` ему соответствует». Тот же вывод записан в handoff-notes п.1 и в spec R32.
Ветка IPC взяла только первую половину: `asRecipeName('constructor')` возвращает
`'constructor' as RecipeName`.

Почему это не косметика. `asRecipeName` — единственный рантайм-страж имени на границе
сокета (И5). Демон дальше делает `manifest.tools[recipeName]`; `manifest.tools` — обычный
объект из `doc.toJS()`, поэтому `tools['constructor']` резолвится по цепочке прототипов в
`Object` — истинное значение, не являющееся `Recipe`. Проверка вида
`const recipe = tools[name]; if (!recipe) deny;` её пропустит. Схема этот вход закрывает,
брендированный конструктор — нет.

Комментарий `ipc.ts:18-20` при этом утверждает: «Та же форма, что у `propertyNames` схемы
манифеста… Совпадение двух копий проверяет тест — иначе они разъедутся». Копии уже
разъехались, а тест (`src/tool.test.ts:108-114`) сравнивает только
`RECIPE_NAME_PATTERN.source` с `propertyNames.pattern` и половину `not/enum` не смотрит
вовсе. Заголовок соседнего кейса — «asRecipeName отвергает то, что отвергает схема»
(`tool.test.ts:116`) — для `constructor` ложен.

Fix: в `asRecipeName` добавить тот же явный отказ трём именам и расширить тест парности до
обеих половин `propertyNames` (сравнивать и `pattern`, и `not.enum`), а не только `source`.

### Minor — `refine` не проверяет `defaults` вовсе, док утверждает обратное

`packages/contracts/src/validate/refine.ts:135`

`refine()` обходит только `Object.entries(manifest.tools)`. Ни `checkNoSubstitution`, ни
`checkDenyNonEmpty` до `manifest.defaults.sandbox` не доходят. Значит проходят загрузку:

- `defaults.sandbox.write.allow: ["{}/out"]` — слот `{}` в профиле песочницы;
- `defaults.sandbox.read.deny: []` — пустой `deny` на узле, где он и опасен.

`docs/07-contracts.md` формулирует инвариант без оговорки: «Никакой параметр не может влиять
на `exec`, `cwd` или профиль песочницы: слот `{}` в любом из них — ошибка загрузки».
`src/validate/branch-checks.ts:54,57` вешает `no-param-substitution` на `SandboxProfile` и
`deny-non-empty` на `AccessRule` — обе ветки инстанцируются и внутри `Defaults`, и
комментарий на строках 55-56 отдельно объясняет, что правило поставлено «на носителе данных,
а не на рецепте», — то есть именно так, как в коде не сделано. Тест таблицы
(`src/validate/refine.test.ts:185-197`) сверяет только **множества ключей** с `$defs`, а не
то, что заявленная проверка где-то применяется, поэтому расхождение не краснеет.

Практическая опасность невелика (`{}` в пути — просто каталог с таким именем, пока подстановку
делает только E2 и только в argv), но заявленный инвариант шире кода.

Fix: прогнать профильную половину `checkNoSubstitution` и `checkDenyNonEmpty` по
`manifest.defaults.sandbox` (пустой `defaults.deny` при этом, вероятно, надо оставить
законным — тогда сузить формулировку в доке и приписать в `branchChecks` пометку
«рецептные только»).

### Minor — проекция OTLP теряет данные молча

`packages/contracts/src/otlp.ts:97-116`

`toOtlp` не эмитит `sandbox.profile`, `risk.annotations` и `chain` вообще, а
`sandbox.violations` и `redactions` отдаёт **длиной массива** под именами
`mcpproxy.sandbox.violations` и `mcpproxy.redactions` — ровно теми, которыми старая версия
`docs/07-contracts.md` называла сами массивы (см. удалённые строки диффа: `"mcpproxy.sandbox.violations": [ {...} ]`).

Ни `docs/07-contracts.md` (раздел «Экспорт в OTLP»), ни `docs/adr/0003-otel-event-schema.md`
об этом не говорят; ADR по-прежнему держит «✅ Бесплатный экспорт в любой observability-стек».
В `otlp.test.ts` утверждений о полноте нет.

Fix: одна строка в докe — «OTLP-спан это сводка, полная запись живёт в JSONL; профиль,
аннотации и цепочка в спан не уезжают» — и переименовать два атрибута в
`mcpproxy.sandbox.violations.count` / `mcpproxy.redactions.count`, чтобы имя не обещало
список. Имена атрибутов — значения, а не экспорты, снапшот поверхности их не морозит.

### Minor — `..`-проверка root даёт ложное срабатывание на каталоге вида `..name`

`packages/contracts/src/validate/refine.ts:119-120`

```ts
const outside = relative(manifestDir, resolved);
if (outside.startsWith('..')) report(path, 'относительный root не может выходить за каталог манифеста');
```

Классический промах: `root: "./..cache"` резолвится в `<manifestDir>/..cache`,
`relative` возвращает `'..cache'`, `startsWith('..')` истинно — и легитимный **подкаталог**
объявляется выходом за пределы. Настоящий выход (`'../x'`, `'..'`) при этом ловится верно,
то есть дефект односторонний: ложный отказ, не ложное разрешение.

Fix: `if (outside === '..' || outside.startsWith(`..${sep}`))` с `sep` из `node:path`.

### Minor — санитайзер склеивает слова на табуляции, ровно как обещал не делать

`packages/contracts/src/tool.ts:83-84`

```ts
// Переводы строки схлопываются ДО вырезания невидимых: иначе `a\nb` склеилось бы в `ab`.
const flattened = withoutAnsi.replace(/[\r\n]+/g, ' ');
const visible = flattened.replace(INVISIBLE, count);   // INVISIBLE = /[\p{Cc}\p{Cf}]/gu
```

В `\p{Cc}` входят не только `\r\n`, но и `\t` (U+0009), `\v` (U+000B), `\f` (U+000C).
Схлопывается только первая пара, остальные три просто удаляются, поэтому
`"колонка\tзначение"` → `"колонказначение"` — тот самый дефект, ради предотвращения которого
комментарий строкой выше и объясняет порядок операций. Комментарий описывает намерение,
которого код не выполняет для трёх символов из четырёх.

Fix: `replace(/[\r\n\t\v\f]+/g, ' ')` (или `/\s+/gu`, раз следующим шагом всё равно идёт
схлопывание двойных пробелов).

### Minor — `canonicalizeJcs` рекурсивна без ограничения глубины, а на входе недоверенный JSON

`packages/contracts/src/jcs.ts:60,74` — и через `src/audit/args.ts:19`

`argsHash(recipeName, params)` хэширует `params`, а `params` — это
`IpcRequest.params: Readonly<Record<string, unknown>>` (`src/ipc.ts:47`), то есть JSON,
приехавший по сокету. `canonicalizeJcs` спускается рекурсивно без счётчика глубины, поэтому
вложенность в несколько десятков тысяч уровней (тривиально кодируется в JSON) даёт
`RangeError: Maximum call stack size exceeded` внутри демона. Каждый другой отказ в этом
файле — намеренный `TypeError` с внятным текстом; этот единственный вылетает движком.

Правка не ломает замороженную поверхность (сигнатура не меняется), но её проще внести сейчас,
пока файл в фокусе.

Fix: протащить `depth` через рекурсию и бросать `TypeError` за задокументированным потолком.

### Minor — три замороженных дайджеста ни с чем не связаны кодом пакета

`packages/contracts/src/lock.ts:113-123, 249-269`

`diffLock` строит все четыре слота исключительно из `entry.snapshot` и не смотрит ни в
`LockEntry.recipeHash`, ни в `LockFile.manifestHash`. `LockStatus` и `LockCheck` объявлены и
экспортированы, но **ни одна функция их не возвращает** и ни один тест их не трогает
(проверено grep'ом по `src/`: единственные упоминания — само объявление и комментарий в
`annotations.ts:32`). Lock, у которого записанный `recipeHash` противоречит собственному
`snapshot`, даёт чистый дифф во всех четырёх слотах.

Комментарий `lock.ts:84-85` («Поле называется `recipeHash`… замороженная формула носит это
имя») и `docs/07-contracts.md` подают дайджест как якорь `lock_check`. Фактически в E0 он
только объявлен.

Fix: либо добавить чистую `checkLock(lock, manifest): LockCheck` рядом с `diffLock` (ей ничего,
кроме уже написанного, не нужно), либо дописать в doc-комментарий к `LockCheck`, что сверку
дайджестов делает E1/E2 и `diffLock` её не выполняет — сейчас читатель вправе решить обратное.

### Minor — шесть кейсов заглушек проходят вхолостую при провале разбора

`packages/contracts/src/recipes.test.ts:30, 35, 48, 50, 63` (паттерн `if (!result.ok) return;`)

`result` вычисляется на уровне модуля один раз. Первый кейс (строка 14) при провале
`parseManifest` бросает и краснеет — а остальные шесть **возвращаются молча и считаются
пройденными**. То есть поломка `recipes/mcpproxy.yaml` даёт один красный тест вместо семи, и
все утверждения о тирах, матчерах и И2 исчезают вместе с диагностикой.

Fix: перенести разбор в `beforeAll`, бросающий при `!ok`, — тогда все семь кейсов падают на
общей причине; либо заменить `return` на `throw`.

### Nit — недостижимая ветка `?? 1` в `durationToMs`

`packages/contracts/src/lock.ts:23`

```ts
return Number(match[1]) * (UNIT_MS[match[2] as string] ?? 1);
```

Регулярка строкой выше уже сузила `match[2]` до `ms|s|m|h`, и все четыре — ключи `UNIT_MS`.
Ветка `?? 1` недостижима; при этом её семантика («неизвестная единица = миллисекунды»)
противоположна духу функции, которая на строку выше бросает на любой неразобранной строке.

Fix: `const unit = UNIT_MS[match[2]!]; if (unit === undefined) throw new TypeError(...)`.

### Nit — нормализация дедуплицирует `deny` непоследовательно

`packages/contracts/src/lock.ts:141` (`mergeAccess` дедуплицирует) против `146` (`accessOf` — нет)

`normalizeDefaults` пропускает списки как есть, `mergeAccess` дедуплицирует объединённый
`deny`. Значит `defaults.sandbox.read.deny: ["~/.ssh", "~/.ssh"]` попадает в
`lock.defaults` с дублем и в каждый `snapshot.effective` — без. `allow` не дедуплицируется
нигде. Детерминировано (хэши не флакают), но «нормализованное представление» нормализовано не
до конца.

Fix: дедуплицировать и в `accessOf`, либо снять дедупликацию в `mergeAccess` и записать, что
кратность и порядок — часть формы.

### Nit — `slice` по кодовым единицам режет суррогатную пару

`packages/contracts/src/tool.ts:87`

`collapsed.slice(0, DESCRIPTION_MAX_LENGTH)` работает по UTF-16 code units, поэтому описание,
у которого на 1024-й позиции стоит эмодзи, уезжает в `tools/list` с одиночным суррогатом.
Отдельно: срез идёт **после** `trim()`, так что на границе может снова появиться хвостовой
пробел. Иронично рядом с `jcs.ts:18`, который одиночный суррогат считает достаточным
основанием бросить.

Fix: резать по кодовым точкам (`[...collapsed].slice(0, N).join('')`) и `trimEnd()` после среза.

---

## 2. Соответствие конвенциям проекта

Проверено против `docs/07-contracts.md` (конвенция письменная) и `src/deps.test.ts` /
`api-surface.snapshot.txt` (конвенция исполняемая).

- **`.d.ts` не ссылается на внешнюю библиотеку** — соблюдено. `src/validate/index.ts:1-2`
  импортирует `ErrorObject` из `ajv` и `Document`/`LineCounter` из `yaml`, но только в
  сигнатурах приватных `diagnose`/`buildMatchers`, поэтому declaration emit их не выносит:
  `api-surface.snapshot.txt:953-956` показывает `validate/index.d.ts` из двух строк,
  импортирующих только `../types.js`. `ajv`/`yaml`/`re2`/`@modelcontextprotocol/sdk` в
  снапшоте встречаются исключительно внутри текста комментариев.
- **`ajv`/`yaml`/`re2` не текут в корневой вход** — `src/index.ts:20-31` реэкспортирует
  только модули без этих зависимостей; `deps.test.ts:69-93` держит границу с обеих сторон
  (включая «`./audit` тянет `node:crypto` — **и только его**»).
- **Снапшот поверхности** — секция схемы внутри `api-surface.snapshot.txt` (строки 958+)
  побайтово совпадает с `schema/mcpproxy.schema.json` (проверено `diff`, расхождений нет).
  Часть с `.d.ts` проверить без сборки нельзя — это остаётся за гейтом `build-test`.
- **Размещение по правилу «нужен `node:crypto` → `./audit`»** соблюдено: `jcs.ts`,
  `lock.ts`, `event.ts` (включая `ChainedEvent`) — в корне, `chain/lock/args` — в `./audit`.

Findings по этому измерению: нет.

---

## 3. История коммитов

`git log --oneline c65f71ea..HEAD` — 23 коммита: девять плановых/ревью-артефактов и
двенадцать реализационных, по одной задаче на коммит.

- **Тихо отменённых или переписанных изменений не найдено.** Единственная правка уже
  закоммиченного артефакта — `5a6bb0b`, добавляющий рецептные `env`/`output` в
  `schema/mcpproxy.schema.json` и `manifest.generated.ts` поверх `6d4d802`. Она **расширяет**
  контракт, а не отменяет решение, объяснена в теле коммита, продублирована в
  `handoff-notes.md` п.10 как единственное расширяющее расхождение, и доехала до финального
  дерева целиком: поля есть в схеме, в сгенерированных типах, в `normalizeRecipe`
  (`lock.ts:186-190`) и в таблице слияния `docs/07-contracts.md`. Это то, по чему нужно
  решение владельца, а не дефект.
- **Утверждений коммитов, которых нет в дереве, не найдено.** Выборочно сверено:
  «`propertyNames` + `not/enum`» → `schema:18-21`; «`AJV_OPTIONS` в отдельном модуле» →
  `src/validate/ajv.ts:20-29`; «третья форма `exec[0]`» → `refine.ts:55-59`;
  «`ChainedEvent` в `event.ts`» → `event.ts:93-95`; «нормализация кавычек в снапшоте» →
  `api-surface.test.ts:58`; «184 теста в 15 файлах» → 15 файлов `*.test.ts` на диске.
- `def7d01` («хендовер») трогает только `docs/vibe-coding/**` — код не менялся после заморозки
  в `2ef1541`.

Findings по этому измерению: нет (с оговоркой, что расширение схемы из `5a6bb0b` требует
явного «да» владельца до мержа — так и просит сам коммит).

---

## 4. Комментарии в коде

- **R23 выполнен.** `git show c65f71ea:packages/contracts/src/index.ts` содержал
  `// TODO(E0): JSON Schema манифеста, схема события (OTel GenAI), три рецепта-заглушки.` —
  в HEAD его нет. `grep -rn "TODO\|FIXME"` по `packages/contracts/{src,schema,scripts,recipes}`
  не даёт ни одного совпадения.
- **Закомментированного кода не найдено** ни в одном изменённом файле.
- **Комментарии, которые расходятся с кодом**, вынесены выше как самостоятельные находки:
  `types.ts:31-37` и `regex.ts:14-16` (Major №1), `ipc.ts:18-20` (Major №2),
  `branch-checks.ts:55-56` (Minor про `defaults`), `tool.ts:82` (Minor про табуляцию),
  `lock.ts:84-85` (Minor про дайджест-якорь).
- Проверены и оказались **верными** несколько комментариев, которые легко было бы написать
  неточно: `lock.ts:168-169` (порядок ключей `params` не переставляется, потому что
  `propertyNames` запрещает имена-числа — действительно, `^[a-z]` исключает ведущую цифру);
  `jcs.ts:69-70` (сортировка `Array.prototype.sort` по строкам = порядок кодовых единиц
  UTF-16); `ajv.ts:6-8` (`../../schema/…` одинаково резолвится из `dist/validate` и
  `src/validate`); `otlp.ts:7-15` (различие «имена полей JSON» и «значения `key`»);
  `chain.ts:31-34` (`chain?: never` действительно ловит `ChainedEvent` компилятором).
  `isoToUnixNano` (`otlp.ts:53-60`) отдельно проверен на отрицательных эпохах и дробях
  длиннее трёх знаков — арифметика верна, `Math.floor` и добавление положительных наносекунд
  согласуются.

---

## 5. Доки против кода

- **Атрибуты OTLP.** Таблица в `docs/07-contracts.md` («Экспорт в OTLP») и правка
  `docs/adr/0003-otel-event-schema.md` перечисляют шесть реестровых имён —
  `gen_ai.operation.name`, `gen_ai.tool.name`, `network.transport`, `mcp.session.id`,
  `mcp.method.name`, `mcp.protocol.version`. `src/otlp.ts:74-81` эмитит ровно эти шесть, с
  теми же константами (`'pipe'`, `'tools/call'`, `MCP_PROTOCOL_VERSION = '2025-11-25'`), и
  `otlp.test.ts:113-118` отдельно утверждает отсутствие `mcp.tool.name`, `mcp.request.id`,
  `mcp.transport`, `mcp.resource.uri`, `jsonrpc.request.id`. Совпадение полное, включая
  правку направления переезда OTel в `docs/04-research-findings.md`. **Расхождений нет** —
  кроме неполноты по `mcpproxy.*`, вынесенной выше отдельным Minor.
- **Таблица риск-тиров.** `docs/07-contracts.md` и `docs/adr/0004-mcp-tool-annotations.md`
  описывают четыре строки с оговоркой «`destructiveHint` значим только при
  `readOnlyHint == false`» — `src/annotations.ts:34-46` реализует это буквально, включая
  ранний выход на `readOnly`. Формулировка «fail-safe by construction» вычищена из обоих
  документов и заменена на ту же узкую границу, что стоит в doc-комментарии функции.
- **Формулы дайджестов.** `docs/07-contracts.md`, `docs/02-architecture.md`,
  `docs/08-demo-scenarios.md` и `docs/adr/0006-manifest-lockfile.md` приводят три формулы и
  кодировку «64 строчных hex без префикса `sha256:`». Совпадает с `audit/chain.ts:28,36`,
  `audit/lock.ts:19,28`, `audit/args.ts:19`; вектор кодировки зафиксирован в
  `chain.test.ts:45`. Пример lock-файла в докe обновлён под реальные поля
  (`recipeHash`/`snapshot`/`defaults`).
- **Таблица слияния с `defaults`.** Все семь строк сверены с `normalizeRecipe`
  (`lock.ts:193-205`): замена по листу для `allow`/`env.allow`/скаляров, объединение для
  `deny`, `?? ` для отсутствующего ключа, пустой `allow` как «обнулить». Соответствует.
- **`docs/07-contracts.md` про IPC:** «Лишнее поле в этой форме тоже не компилируется: `argv`
  сюда не приписать». Утверждение верно только для литералов на стороне TypeScript
  (excess property check) и ничего не значит для JSON, приехавшего по сокету, — рантайм-валидатора
  `IpcRequest` пакет не даёт (есть только `asRecipeName`/`asSessionId`, и первый — с дырой из
  Major №2). Абзац стоит рядом с утверждением про «полный контроль над сокетом», из-за чего
  читается сильнее, чем есть. **Nit**, fix: приписать «на границе типов» или сослаться на то,
  что рантайм-разбор — дело E4/E5.
- **`WORK.md:13-19` — Nit.** Коммит `2ef1541` («заморозка контракта») отметил галочкой только
  «Четыре рецепта-заглушки». Пункты «JSON Schema манифеста», «Схема события», «MCP tool
  annotations» и «**Заморозка**» остались `- [ ]`, хотя все четыре доехали в этой же ветке
  (`schema/mcpproxy.schema.json`, `src/event.ts`, `src/annotations.ts`,
  `api-surface.snapshot.txt`). Чек-лист теперь занижает состояние. Fix: отметить их тем же
  коммитом, который объявляет заморозку.
