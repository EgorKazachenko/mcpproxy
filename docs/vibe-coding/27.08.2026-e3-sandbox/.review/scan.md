# Multi-dimension scan — E3 executor and sandbox

codeTree: `f9e758fc1aa80c7d7970a395b640028cc9981eb6`

Ветка: `v2/e3-sandbox`, диапазон `e40b7de..HEAD`, поверхность — `packages/core/src/exec/**`, `packages/core/vitest.config.ts`, `docs/02`, `docs/03`, `docs/10`.
Сканировано по шести измерениям: конвенции репозитория, широкий баг-скан, консистентность с `packages/contracts`, мёртвый код и дубли, правдивость доков, гигиена истории.

Общая оценка перед списком: код держит планку репозитория — комментарии по-русски, объясняют **почему**, а не **что**, и почти каждый несёт проверяемую ссылку (я выборочно проверил `event.ts:92-93`, `event.ts:100`, `event.ts:116`, `event.ts:149`, `event.ts:156`, `lock.ts:60`, `lock.ts:68`, `lock.ts:239-244`, `ipc.ts:13`, `manifest.generated.ts:56`, `07-contracts.md:154`, `07-contracts.md:365`, `10-honest-limitations.md:14`, `10-honest-limitations.md:84` — все точны). Находки ниже — то, что из этой планки выпало.

## Major

- `packages/core/src/exec/srt-manager.ts:315-316` (в паре с `:339-346`) — **Major**. `applyNetwork(base, IDLE_NETWORK)` стоит только на успешном пути. Если `options.body` бросил (в `none` это ровно обычный случай — `runProcess` реджектится по `error`/ENOENT, когда `command[0]` рецепта не существует; в обоих режимах — бросок `srt.wrap` или `'srt вернул пустой argv'` из `modes/seatbelt.ts:222`), управление уходит в `finally`, семафор освобождается, а **глобальный конфиг srt остаётся с политикой упавшего вызова**. В `none` это `allowedDomains: ['*']` (`modes/none.ts:77`). R52 и `srt-manager.ts:39-42` требуют, чтобы идловое состояние allowlist было пустым списком именно потому, что фоновый потомок не должен попасть под чужую политику; на пути ошибки требование не исполняется. Интеграционное утверждение `modes/seatbelt.test.ts:265` («в простое allowlist пуст») проверяет только успешный путь. Фикс: перенести `await delay(DRAIN_WINDOW_MS); applyNetwork(base, IDLE_NETWORK);` в `finally`, выполнив их до `release()` и на успешном, и на аварийном пути (снятие политики обязано случиться раньше освобождения семафора — это и есть порядок R46).

- `packages/core/src/exec/modes/none.ts:40-52` — **Major**. `proxyEnvFor` отдаёт то, что вернул `generateProxyEnvVars`, без единой проверки. Вендор (`node_modules/@anthropic-ai/sandbox-runtime/dist/sandbox/sandbox-utils.js:444-446`) при `httpProxyPort === undefined && socksProxyPort === undefined` возвращает `['SANDBOX_RUNTIME=1']` — **без `HTTP_PROXY`, без `NO_PROXY`, без токена**. То есть если `SandboxManager.initialize` резолвнулся, но прокси не поднялся (Linux — `assertModeSupported` пропускает `none` на любой платформе и это закреплено тестом `sandbox.test.ts:25`; будущая версия вендора с ленивым стартом прокси; занятые порты), режим `none` тихо превращается из «наблюдающего baseline» в **слепой**: ноль violations, сеть ребёнка полностью открыта, все тесты зелёные, а S5 показывает нулевую эксфильтрацию как успех. Это ровно тот класс отказа, против которого в этом же коде стоит `assertWildcardSurvived` (`srt-manager.ts:448`) и терминальный флаг R50. Фикс: в `proxyEnvFor` бросать, когда `srt.proxyPort() === undefined && srt.socksPort() === undefined` (и отдельно — когда `srt.caTrustBundlePath() === undefined`, иначе HTTPS в baseline падает как сетевая ошибка, что `modes/none.ts:33-34` само называет худшим исходом), с текстом в стиле `DISPOSED_MESSAGE`.

- `packages/core/src/exec/srt-manager.ts:178` (запись — `:258`, очистка — только `:396` и `:408`) — **Major**. Массив `telemetry` растёт неограниченно: одна запись на **каждый разрешённый запрос** за всю жизнь процесса демона, очищается лишь в `dispose()`. При этом `tlsTerminate` включён (`modes/seatbelt.ts:71`), то есть `request.url` — это **полный HTTPS-URL с query-строкой**, что сам же `docs/10-honest-limitations.md:113` объявляет чувствительным содержимым. Получается неограниченный рост памяти в долгоживущем демоне плюс бессрочное удержание URL с секретами в query — при том что у `telemetrySnapshot()` (`:279`) нет ни одного потребителя во всём воркспейсе и ни одного теста, а те же данные уже доезжают до потребителя как `SandboxViolation{action:'allowed', bytes}` (`:261-268`). Фикс: удалить поле `telemetry` и метод `telemetrySnapshot()` целиком; если снапшот нужен E6/E8 — очищать массив в начале `withNetworkPolicy` (рядом с установкой `this.active`), чтобы он жил ровно один вызов.

## Minor

- `packages/core/src/exec/events.ts:77-81` — **Minor**. `FIELD_FIRST_STAGE` экспортирован, не импортируется нигде (проверено по всему `packages/**`), не покрыт ни одним утверждением и не реэкспортирован из `exec/index.ts`. Это декларация порядка полей, которую **некому исполнить**: реальный порядок задаёт `runInMode` (`modes/seatbelt.ts:138`, `:144-148`, `:173-177`), а проверяет его `events.test.ts:197-215` вручную, не ссылаясь на константу. Фикс: либо удалить константу, либо переписать `events.test.ts:197-215` так, чтобы оно итерировалось по `FIELD_FIRST_STAGE` и проверяло «поле не появилось раньше своей стадии» для каждой записи — тогда константа станет несущей.

- `packages/core/src/exec/srt-manager.ts:400-409` — **Minor**. `resetForTests()` помечен «Только для тестов», но его не зовёт ни один тест (проверено grep'ом по `packages/**`) — это мёртвый метод в продовом классе. Хуже того, он обнуляет `initPromise`, **не** вызвав `this.unsubscribe?.()`: если бы его когда-нибудь позвали, следующий `doInitialize` (`:216`) подписал бы на стор второго слушателя, и каждое нарушение считалось бы дважды. Фикс: удалить метод; изоляция между тест-файлами уже обеспечена `isolate` по умолчанию у vitest в паре с `fileParallelism: false`.

- `packages/core/src/exec/srt-manager.ts:411-413` — **Minor**. `isPoisoned()` не имеет ни одного потребителя и ни одного теста; ветка отравления по признанию `handoff-notes.md` не покрыта. Аксессор без вызывающего создаёт иллюзию наблюдаемости состояния. Фикс: удалить метод, а состояние отравления сделать наблюдаемым там, где оно нужно — через текст `SrtManagerError`, который уже бросается из `:294`.

- `packages/core/src/exec/index.ts:10-13` — **Minor**. Докстринг обещает, что каждый экспорт здесь есть потому, что «его зовёт другой эпик», и называет потребителя ровно для четырёх: `buildProfile`, `policyHash`, `collapseOutput`, парсер нарушений. Из 25 реэкспортированных символов **13 не имеют ни потребителя во всём воркспейсе, ни названной причины**: `MANDATORY_DENY_FILES`, `MANDATORY_DENY_DIRECTORIES`, `MANDATORY_DENY_GIT_PATHS`, `mandatoryDenyGlobs`, `resolveProfilePath`, `toSandboxProfile`, `MINIMAL_PATH`, `truncateToBytes`, `DEFAULT_GRACE_MS`, `EXEC_STAGES`, `measure`, `measureAsync`, `assertModeSupported`. Публичная поверхность, которую никто не звал, — это то, что придётся поддерживать вечно. Фикс: сократить список до символов с названным потребителем (`createSandbox`, `newCommandId`/`asCommandId`, типы `Sandbox`/`ExecRequest`/`ExecOutcome`/`Termination`/`StreamOutcome`/`ExecEvent`/`EventSink`, `buildProfile`, `policyHash`, `ResolvedSandboxPolicy`, `collapseOutput`, парсер нарушений), остальное оставить экспортом модуля для тестов, не поднимая в `exec/index.ts`.

- `packages/core/src/exec/` (весь каталог) vs `packages/contracts/src/api-surface.test.ts` — **Minor**. `contracts` держит свою поверхность снапшотом с `.d.ts` и краснеет на любом новом экспорте; `core` после E3 стал вторым пакетом с намеренно выбранной поверхностью (`exec/index.ts`), но аналога у него нет. Изоляция вендора закрыта обходом графа (`events.test.ts:98-146`) — это аналог `deps.test.ts`, — а вот случайный `export *` или новый символ в `exec/index.ts` проедут молча. Фикс: добавить в `packages/core` тест по форме `packages/contracts/src/api-surface.test.ts`, снимающий снапшот со списка экспортов `dist/index.d.ts`.

- `packages/core/src/exec/modes/seatbelt.ts:155` вместе с `packages/core/src/exec/profile.ts:135` — **Minor**. `mandatoryDenyGlobs` считается дважды за вызов, из двух мест, по независимо собранным входам: один результат уезжает в **принуждение** (`write.deny` профиля), второй — в **классификацию** (`ClassifyPolicy.mandatoryPaths`, то есть бейдж `mandatory-deny` из S6). Сегодня входы совпадают побайтово, но связи между ними нет никакой, и правка одной стороны разведёт бейдж с реальной политикой молча — а `violation.test.ts` строит `mandatoryPaths` руками и этого не увидит. Фикс: добавить в `ResolvedSandboxPolicy` (`profile.ts:13-18`) поле `readonly mandatory: readonly string[]`, заполнять его в `buildProfile` тем же вызовом, что и `write.deny`, и в `runInMode` брать `profile.value.mandatory` вместо повторного счёта.

- `packages/core/src/exec/profile.ts:113-118` — **Minor**. Обоснование отдельного параметра `writeRoots` («вызывающий вправе анкерить запреты и на `getDefaultWritePaths()`») не исполняется ни одним вызывающим: `modes/seatbelt.ts:142-143` передаёт ровно `effective.sandbox.write.allow`, и `docs/10-honest-limitations.md:119` прямо фиксирует, что якорь — именно `write.allow`. Тесты (`profile.test.ts:49`, `:65`, `:71`) гоняют параметр значениями, которых продакшен-путь не порождает, то есть покрывают гибкость, а не поведение. Фикс: убрать параметр и выводить корни из `sandbox.write.allow` внутри `buildProfile` — сигнатура тогда выражает то, что обещает проза, — либо, если якорь по умолчанию действительно должен быть шире, начать передавать `getDefaultWritePaths()` из `runInMode` и закрепить это тестом.

- `packages/core/src/exec/limits.ts:258-269` vs `packages/core/src/exec/limits.ts:126-132` — **Minor**. `terminationOf` решает по `produced > maxBytes` (**до** редакции), а `StreamOutcome.truncated` — по `buffer.byteLength < redacted.byteLength` (**после** неё). Когда E6 подключит `redact` через объявленный слот (`limits.ts:49`), появится исход `termination: 'output-cap'` при `stdout.truncated === false && stderr.truncated === false` — то есть ничего не отброшено (пример: `produced = 70`, `maxBytes = 64`, `holdBackBytes = 8`, редактор укоротил окно до 10 байт). `Termination` — дискриминатор, из которого E4 по D6/R18 выводит вердикт, и «отказ, потому что вывод обрезан» на вызове, где не обрезано ничего, — не тот вердикт. `limits.test.ts:194-202` фиксирует ровно половину этой пары (что `truncated` не ставится зря), про `termination` не утверждая ничего. Фикс: считать `terminationOf` по результату `finish()` (`droppedAtRead || droppedAtCap`), а не по `produced`, и добавить в `limits.test.ts` утверждение на связку `termination === 'output-cap' ⇔ хотя бы один поток truncated`.

- `packages/core/src/exec/profile.ts:24` — **Minor**. Комментарий утверждает, что детектор дрейфа копии `DANGEROUS_FILES` стоит в `srt-manager.test.ts` (R10). Он стоит в `modes/seatbelt.test.ts:148` («детектор дрейфа: вендорский набор обязательных запретов не изменился»); в `srt-manager.test.ts` нет ни одного упоминания вендорского списка. Читатель, пришедший проверить копию, находит файл без детектора и делает вывод, что детектора нет. Фикс: заменить ссылку на `modes/seatbelt.test.ts` (там же лежит `RECORDED_VENDOR_DENIALS`).

- `packages/core/src/exec/netpolicy.test.ts:38-39` — **Minor**. Тот же класс: комментарий отправляет за наблюдаемой проверкой «хост в обоих списках отказан живым запросом» в `srt-manager.test.ts`, а она в `modes/seatbelt.test.ts:214-219` («network.deny рецепта доезжает до deniedDomains и бьёт allow (R53)»). `handoff-notes.md` (§7) описывает раскладку правильно — расходится именно код. Фикс: заменить ссылку на `modes/seatbelt.test.ts`.

- `packages/core/src/exec/sandbox.ts:143` — **Minor**. Текст ошибки, который увидит пользователь при `mode: 'container'`, ссылается на `ADR-0007` — это «Доменный allowlist сети вместо бинарного deny», о контейнерах там ни слова. Заглушку `container` объявляет `docs/adr/0002-sandbox-runtime.md` («Реализуем три режима: `none`, `seatbelt`, `container` (заглушка)»). Фикс: заменить `ADR-0007` на `ADR-0002` в строке ошибки.

- `docs/06-epics.md:15` — **Minor**. Строка E3 по-прежнему перечисляет `rlimits` в составе эпика, тогда как этот же диплом ветки добавил в `docs/03-threat-model.md:63` и `docs/10-honest-limitations.md:109` явное «**Настоящих `setrlimit` нет**». Старое утверждение, которому код теперь противоречит, оставлено стоять — а `docs/` объявлен единственным источником истины для дека. Фикс: в `docs/06-epics.md:15` заменить `rlimits` на `SIGKILL по группе процессов`, слово в слово как в исправленной строке `03-threat-model.md:63`.

- `docs/10-honest-limitations.md:118` — **Minor**. Колонка называется «Замер», а в ячейке стоят «П6a, П9». П6a (`probes.md:168-185`) измеряет **лаг** доставки нарушения и опровергает блокер, П9 (`probes.md:239`) проверяет `updateConfig`; коалесценцию файловых отказов по чанкам `log stream` не измеряет ни одна проба — утверждение приходит из `plan.md:905-906`, где оно тоже без замера. Строка «нижняя оценка» верна по существу, но её основание — не замер. Фикс: заменить «П6a, П9» на «чтение `macos-sandbox-utils.js`» (как уже сделано в строке `:117`) или снять пробу, считающую файловые отказы в цикле, и подставить её вывод.

- `docs/10-honest-limitations.md:114` — **Minor**. Ячейка «Замер» цитирует `П1: NO_PROXY=...`, но `probes.md` §П1 (`:6-40`) этой строки не содержит — там напечатаны `argv[0]`, `argv.length`, `argv[1]`, тождественность `env`, код выхода, stderr и три строки нарушений. Значение зафиксировано в `plan.md:168`, а не в `probes.md`, на который ссылается шапка таблицы (`:105`). Читатель, пошедший проверять по указанному адресу, ничего не найдёт. Фикс: указать источник как `plan.md:168` либо дописать в `probes.md` §П1 фактический вывод `argv[2]` с `NO_PROXY`.

- `docs/10-honest-limitations.md:120` — **Minor**. Строка про `setsid` утверждает «мы подтверждаем пустоту группы ограниченным опросом и **громко отказываем**» — но именно для сбежавшего через `setsid` громкого отказа не будет: `confirmGroupEmpty` (`limits.ts:167-174`) спрашивает `process.kill(-pid, 0)`, процесс в новой группе там уже не виден, `groupDrained` приезжает `true`, семафор освобождается штатно, и вызов завершается тихо и зелено. Громкий отказ (R52, `srt-manager.ts:326-333`) срабатывает только для потомка, **оставшегося в группе**. В доке «честных границ» это ровно та неточность, которую документ существует, чтобы не допускать. Фикс: переписать ячейку так: сбежавший в новую группу невидим для проверки пустоты, `groupDrained` для него `true`, и вызов завершается без следа; громкий отказ покрывает только выживших внутри группы.

- `docs/10-honest-limitations.md:110` — **Minor**. Заголовок строки говорит «В цепочке на macOS есть `sh -c`», а тело и замер П1 — `/bin/bash -c`. Разные оболочки с разной семантикой; в документе про честные границы точность имени бинаря — не придирка. Фикс: заменить заголовок на «В цепочке на macOS есть `bash -c`».

- git history, коммит `1951daf` — **Minor**. Сообщение «E3: синглтон srt, принуждение сети через updateConfig, режим seatbelt» не называет `packages/core/src/exec/events.ts` (81 строка, целиком модуль стадийных событий), который в этом коммите и появился; при этом следующий коммит `dbf4ce2` озаглавлен «события стадий, …», хотя `events.ts` уже существовал и он добавляет только `events.test.ts` и `exec/index.ts`. `git log --oneline` в этом месте вводит в заблуждение о том, где искать модуль. Остальные 25 сообщений ветки описывают содержимое точно (сверено `git log --stat`). Фикс: при следующей перебазировке ветки дописать `events.ts` в тему `1951daf` и переименовать `dbf4ce2` в «тесты стадийных событий, изоляция вендора, экспорт из core».

---

## Резолюция

Разбор проведён после прогона, реализовавшей сессией. Правки — коммит `9f2d594`.
codeTree на момент резолюции: `6db5a00a0b4670b0c2c4c2cd3d96de6eb09c9cbb`
(ревью читало `f9e758fc1aa80c7d7970a395b640028cc9981eb6` — анкер выше сохранён намеренно).

Обе Major про аварийный путь и про `telemetry` совпали с находками внутреннего ревьюера;
разбор по ним — в `internal.md`, здесь только то, что нашла эта размерность.

- **Major, `proxyEnvFor` тихо отдаёт env без прокси** — принят, и подтверждён по исходнику
  вендора: при обоих портах `undefined` возвращается ровно `['SANDBOX_RUNTIME=1']`. Добавлены
  оба броска, включая отдельный на отсутствие трастового бандла.
  **Первая мутация здесь выжила** — и это оказалось важнее самой находки: на macOS прокси
  поднимается всегда, поэтому обе ветки отказа были недостижимы из тестов, и «громко»
  оставалось словом. Проверки вынесены в чистую `proxyEnvVars(handles, encodedCommand)`,
  которой можно передать `undefined`; четыре утверждения покрывают обе ветки и границу
  «одного SOCKS-порта достаточно». После этого мутация краснеет.

- **Minor, мёртвая `FIELD_FIRST_STAGE`** — принят, выбран вариант «удалить». Порядок задаёт
  `runInMode`, проверяет `events.test.ts`; константа была третьим описанием того же и
  единственной, которую ничто не исполняло.

- **Minor, мёртвый `resetForTests()`** — принят, удалён. Диагноз про второго слушателя
  верен: метод обнулял `initPromise`, не сняв подписку.

- **Minor, мёртвый `isPoisoned()`** — принят, удалён. Состояние наблюдается текстом
  `SrtManagerError`, как и предлагалось.

- **Minor, поверхность `exec/index.ts` шире, чем обещает докстринг** — принят. Список
  сокращён до символов с **названным** потребителем (E4, E5, E6, E7, E8), и у каждой группы
  экспорта теперь стоит имя эпика, который её зовёт. Тринадцать символов без потребителя
  сняты с публичного входа и остались экспортами своих модулей.

- **Minor, у `core` нет снапшота поверхности** — принят. Добавлен
  `packages/core/src/exec/surface.test.ts` по форме `contracts/api-surface.test.ts`: список
  литеральный, сверяется с `dist/exec/index.d.ts`. Мутация «добавить один экспорт» краснеет.

- **Minor, `mandatoryDenyGlobs` считается дважды** — принят. `ResolvedSandboxPolicy` получил
  поле `mandatory`, заполняемое тем же вызовом, что и `write.deny`; `runInMode` берёт его
  оттуда. Тест утверждает, что поле — подмножество `write.deny`.

- **Minor, параметр `writeRoots` покрывает гибкость, а не поведение** — принят в первой из
  предложенных редакций: параметр убран, корни выводятся из `sandbox.write.allow` внутри.
  Это отступление от подписи, одобренной в плане, и оно осознанное: отдельный параметр
  позволял передать корни, не совпадающие с разрешённой записью, — профиль тогда получил бы
  глобы, якорёные там, где писать всё равно нельзя. Подпись теперь выражает то, что говорит
  R9. Тесты, гонявшие произвольные корни, переписаны на рецепты с соответствующим
  `write.allow`.

- **Minor, расхождение `termination` и `truncated`** — принят; разбор в `internal.md`.

- **Minor, `profile.ts:24` ссылается на файл без детектора** — принят, ссылка исправлена на
  `modes/seatbelt.test.ts`.

- **Minor, `netpolicy.test.ts:38-39` — та же ошибка адреса** — принят, исправлено.

- **Minor, `ADR-0007` в тексте ошибки про `container`** — принят, заменено на `ADR-0002`.
  Проверено: `docs/adr/0007` — доменный allowlist сети, `docs/adr/0002` — песочница.

- **Minor, `docs/06-epics.md:15` всё ещё обещает `rlimits`** — принят, и это отступление от
  буквы плана (он запрещал править `06-epics.md`). Основание плана — «это план работ» — не
  оправдывает оставленное стоять утверждение, которому код теперь противоречит; та же правка
  уже сделана Task 10 в двух других документах. `07-contracts.md` не тронут: он контракт, и
  его неточность зафиксирована требованием R7 и тестом.

- **Minor, `10-honest-limitations.md:118` — «Замер» без замера** — принят, источник заменён
  на чтение `macos-sandbox-utils.js` с явной пометкой «замера коалесценции нет».

- **Minor, `:114` цитирует П1, которого в `probes.md` нет** — принят, источник заменён на
  чтение `sandbox-utils.js`. Дописывать в `probes.md` вывод пробы, которую эта сессия не
  прогоняла, было бы хуже неточной ссылки.

- **Minor, `:120` про `setsid` утверждает громкий отказ, которого нет** — принят и
  переписан: сбежавший в новую группу невидим для `kill(-pid, 0)`, `groupDrained` для него
  `true`, вызов завершается тихо; громкий отказ покрывает только оставшихся в группе. Это
  ровно та неточность, которую документ существует, чтобы не допускать.

- **Minor, `:110` `sh -c` против `bash -c`** — принят, исправлено.

- **Minor, сообщения коммитов `1951daf` и `dbf4ce2`** — находка верна, **правка не
  сделана**, и это осознанный отказ, а не пропуск. Переписывание сообщений требует
  интерактивного rebase, недоступного в этом окружении, а неинтерактивная перезапись восьми
  коммитов ради формулировки темы несёт больше риска, чем стоит устраняемая путаница. Адрес
  `events.ts` записан здесь, в `handoff-notes.md` и в теле PR.

### Пересверка после правок гейтед-размерностей

codeTree `769da6b0b4b23cded1b0b5c0bf7782ce500f47d8` (коммиты `660a4fe`, `7a56442`).

Три оставшиеся размерности ревью — backward-compat, test-quality, error-observability —
внесли правки уже поверх резолюции выше, поэтому дерево сдвинулось второй раз. Резолюция
пересверена по коду, а не объявлена действующей: каждая правка выше проверена присутствующей
в дереве `769da6b0b4b23cded1b0b5c0bf7782ce500f47d8` — массив `telemetry` и `telemetrySnapshot` отсутствуют, снятие политики
стоит в `finally`, перепроверка отравления после `acquire` на месте, `redactUrlForTarget`
применяется к `target`, счётчики атрибуции разведены, `resetForTests`/`isPoisoned`/
`FIELD_FIRST_STAGE` удалены, `mandatory` берётся из политики одним счётом, параметр
`writeRoots` отсутствует, ссылка на ADR исправлена, `rlimits` в `06-epics.md` нет,
событие `spawn` уходит по `onSpawn`.

Ни одна правка последующим коммитом не отменена. Что изменилось внутри уже принятых находок:
поле `attributionMismatches` разведено на `attributionMissing` и `attributionForeign` ещё в
`9f2d594`, а построение записи телеметрии переехало в чистую `telemetryRecord` — по находке
error-observability о том, что сбой счётчика байт уносил саму запись.
