# Журнал мутаций — E2

Каждая строка: правило убрано из кода → какое утверждение покраснело. Прогон ручной,
результат записан здесь, потому что типовой трейс, который ничего не запрещает,
компилируется молча и выглядит как зелёный.

## Задача 1 — раннер

| Мутация | Результат |
|---|---|
| тестовый файл положен на уровень выше `src/` | `× раннер > не имеет тестов за пределами src/` — покраснело |

## Задача 2 — словарь отказов

| Мутация | Результат |
|---|---|
| `scrub` без `sanitizeDescription` (только зачистка суррогатов) | `× вырезает форматирующие символы` — покраснело; канонизация осталась зелёной |
| `scrub` без зачистки суррогатов (только `sanitizeDescription`) | `× канонизуется ВЕСЬ объект` — покраснело; проверка `Cf` осталась зелёной |
| `isCanonicalizable` возвращает `true` всегда | `× совпадает с фактическим поведением canonicalizeJcs` — покраснело |

## Задача 3 — подготовка рецепта

| Мутация | Результат |
|---|---|
| убрана проверка слота в `exec` | `× каждый из четырёх нарушенных инвариантов` — вектор «слот в exec» |
| убрана проверка `root === "/"` | тот же `it`, вектор «root: "/"» |
| убрана проверка относительного `root` | тот же `it`, вектор «относительный root уходит за каталог» |
| убран счёт слотов в `argv` | тот же `it`, вектор «два слота в одном элементе» |
| убран гейт `isCanonicalizable` | 4 красных: exec[0], имя параметра, значение enum, имя рецепта |
| **уровня типа:** `matcher: PatternMatcher \| null` | СБОРКА: TS18047 ×2 + TS2322 на `_matcherNotNullable` |
| **уровня типа:** в ветку `string` добавлено `schema?` | СБОРКА: TS2322 на `_stringBranchClosed` + TS2578 на `@ts-expect-error` |

## Задача 4 — валидация параметров

| Мутация | Результат |
|---|---|
| снят гейт формы контейнера (R29) | 2 красных: четыре не-объекта, `paramName: null` |
| неизвестные ключи не отсеиваются (R6) | 5 красных, включая оба потолка R30а |
| снят гейт `isCanonicalizable` (R28) | `× законный паттерн пропускает одиночный суррогат` |
| снят потолок `VALUE_MAX_CODE_POINTS` (R30) | `× отвергается в string, enum и path` |
| снят потолок на список отказов (R30а) | `× ограниченный список` |
| `matcher.test` переставлен ПЕРЕД гейтами (порядок Ф12) | 2 красных: `вердикт не зависит от строгости автора` и R30 |
| значение вклеено в текст отказа (R25) | `× причина не содержит значения параметра` |
| `Object.hasOwn` заменён на чтение по ключу (R6) | `× объявленный параметр с именем с прототипа` |

## Задача 5 — резолв путей и confinement

| Мутация | Результат |
|---|---|
| `realpath` кандидата убран, проверка только лексическая | 8 красных, включая оба симлинк-вектора A3 |
| `isConfined` переписан на `startsWith` | `× сосед по префиксу не считается лежащим внутри root` |
| `root` не резолвится сам (R14) | 6 красных: законное значение отвергается, три формы, оба argsHash |
| предпроверки нет (`preOk` всегда `true`) | `× оракул существования схлопнут` — коды снова различаются |
| предпроверка ЗАПРЕЩАЮЩАЯ, а не советующая | `× все ТРИ формы одного законного пути приняты` + argsHash |
| пост-проверка confinement убрана | 7 красных, включая все четыре вектора границы |
| `.normalize('NFC')` вставлена в цепочку | `× NFD не схлопывается в NFC` + `× argsHash РАЗЛИЧАЕТСЯ` |

## Задача 6 — сборка argv

| Мутация | Результат |
|---|---|
| склейка вместо поэлементной сборки | 7 красных |
| наивный `String.replace` со строковой заменой (R20а) | 2 красных: `a$'b.log` и `` a$`b.log `` — обе порчи разные |
| `String(value)` вместо явной функции | `× целое не уезжает в экспоненциальную запись` |
| `exec` проходит подстановку (R22) | `× слот в exec возвращается дословно` |
| обход по отсортированным именам (R19) | `× обход идёт по объявлению, а не по алфавиту` |
| `boolean` подставляется вместо присутствия (R21) | `× true добавляет элементы, false не добавляет ничего` |

## Задача 7 — фасад трёх стадий

| Мутация | Результат |
|---|---|
| `timings` не включают отказавшую стадию | `× timings содержат отказавшую стадию` |
| `cwd` отсутствует при отказе на `resolve_paths` | `× отказ на resolve_paths несёт cwd` |
| `cwd` отдаётся и при отказе на `validate` | тот же `it` — вторая половина утверждения |
| наружу отдаётся только первый отказ | `× стадия отдаёт все свои отказы, а не первый` |
| `params` наружу — сырой запрос (R31) | 2 красных: сквозная связка и «значения ПОСЛЕ резолва» |
| **уровня типа:** дорезолвная карта в `buildArgv` | СБОРКА: TS2345, `ValidatedValues` не `ResolvedValues` |
| **уровня типа:** в ветку отказа добавлено `argv?` | СБОРКА: TS2322 на `_failureClosed` + TS2578 на `@ts-expect-error` |

## Задача 8 — корпус, сценарии, перепись

| Мутация | Результат |
|---|---|
| предпроверки нет (`preOk` всегда `true`) | `× S4-а: лексический обход к НЕсуществующей цели` |
| код удалён из `DENIAL_CODES` | `× перепись … не выдумывает лишних` — вектор без кода |
| строка удалена из переписи | тот же `it` — код без вектора |

## Задача 9 — barrel и границы пакета

| Мутация | Результат |
|---|---|
| `import … from '@mcpproxy/contracts/validate'` в `params.ts` | 2 красных, тест называет специфаер |
| `new RegExp('x')` в `params.ts` | `× ни один модуль validate не вызывает конструктор` |
| `dist` удалён | 5 красных, начиная с позитивного контроля |
