# Разбор ревью — E2

Пять независимых измерений против дерева `cad3c3b2`. Отчёты целиком — в `.review/`:
[`internal.md`](.review/internal.md) · [`scan.md`](.review/scan.md) ·
[`backward-compat.md`](.review/backward-compat.md) · [`test-quality.md`](.review/test-quality.md) ·
[`error-observability.md`](.review/error-observability.md).

**Оговорка об инструментах.** `superpowers:requesting-code-review` и плагин
`code-review:code-review`, которые `dual-review` называет двумя всегда-включёнными
ревьюерами, в этом окружении **не установлены**; встроенный `code-review` на высоком усилии
запускает `Workflow`, а на него согласия владельца не было. Поэтому все пять измерений
прогнаны как отдельные Opus-агенты по брифам из `dual-review/SKILL.md`: свой контекст,
read-only по коду, отчёт в файл. Это независимое ревью, но **не те два конкретных
инструмента**, и записано это здесь, а не выдано за них.

**Кругов было два.** Первый — пять измерений против дерева `cad3c3b2`, 42 находки. Второй —
одно чтение **дельты правок** (`6524623d..3a4cc88`) по тем же пяти линзам: оно понадобилось
потому, что два файла, появившихся по требованию самих ревьюеров
(`scripts/check-test-include.mjs` и правка в `contracts/validate/regex.test.ts`), для гейта
были непрочитанным материалом. Это не второй круг по тому же дифу, а первое чтение нового.

Дельта нашла **три регрессии, внесённые правками первого круга**, и четыре места, где правка
не фальсифицировалась ничем. Все закрыты и проверены мутацией.

Итого: 42 + 17 = 59 находок. Исправлено 48, опровергнуто 5, отдано владельцу 6.
Ни одна не пропущена молча.

**Дрейф, который обязан быть назван.** Квитанции пяти измерений привязаны к `3a4cc88` — это
дерево, которое дельта-ревью действительно читало. После него изменились **девять уже
прочитанных файлов**: `contracts/validate/regex.test.ts`, `core/scripts/check-test-include.mjs`,
`core/src/harness.test.ts`, `denial.{ts,test.ts}`, `params.{ts,test.ts}`, `prepare.{ts,test.ts}`.
Это правки по находкам самой дельты, каждая закрыта мутацией (`mutations.md`, раздел «Круг
дельты»), но ревьюер их не читал. Стоит ли третий проход — решение владельца.

## Major

| Находка | Источник | Исход |
|---|---|---|
| `VALUE_MAX_CODE_POINTS` оценивается через `[...value].length`: строка в 100 МБ убивает процесс **фатальным OOM до отказа и до аудита**. Замерено против собранного `dist` | internal, scan | **Исправлено.** Введён `codePointLengthAtMost` — две точные отсечки по единицам UTF-16 (кодовых точек всегда `≥ length/2` и `≤ length`) плюс цикл, обрывающийся на `max + 1`. Замер после: 32 млн единиц — 0.0006 мс против 112 мс и сотен мегабайт кучи |
| Тот же OOM через **ключ** запроса: `clampName` спредил ключ целиком | scan | **Исправлено.** Отсечка перенесена в `scrubName` и делает срез по единицам UTF-16 до всякой зачистки. Замер: 32 млн единиц — 0.06 мс против 70.6 мс |
| `DENIALS_MAX` режет вывод, но не работу: 20 000 неизвестных ключей — 313 мс при бюджете ≤50 мс p95 | scan | **Исправлено.** Цикл обрывается на потолке, остаток считается арифметикой |
| Правило `bounds-satisfiable` — новый **всеманифестный** отказ загрузки на файле, который у установки уже есть; warn-тира нет и завести нельзя | bc | **Исправлено частично, остальное — владельцу.** Инвариант записан в `docs/07-contracts.md` вместе с тем, что отказ всеманифестный и что обновление обязано нести это в релиз-нотах ломающим изменением. Сами релиз-ноты — не в этом дереве |
| Семь правил снимаются с кода при полностью зелёной сюите, включая настоящий выход за границу confinement (`{file: '..'}`) и молчаливое исчезновение всего гейта границ пакета | test-quality | **Исправлено, все семь.** Каждое теперь краснеет — таблица в `mutations.md` |

## Minor

| Находка | Источник | Исход |
|---|---|---|
| `manifestDir` — четвёртый источник строк, не покрытый гейтом R28; вбирается в `cwd` и становится `AuditEvent.cwd` | internal | **Исправлено.** `requireCanonicalizable('каталог манифеста', dir)` + вектор |
| `cwd` не проверяется на выход за каталог манифеста, в отличие от `root` | internal | **Отдано владельцу.** Строка в таблице ограничений спеки. Запрет, которого нет в контракте, заставил бы `prepareRecipe` отвергать манифесты, которые `parseManifest` принимает; владелец рабочего каталога — E3 |
| `clampName` мёртв: `sanitizeDescription` режет раньше, на 1024; тест на R30а вакуумен | internal, scan, error-obs | **Исправлено.** Отсечка стала осмысленной (дешёвая, по единицам, до санитайзера), тест утверждает реальную границу `DESCRIPTION_MAX_LENGTH` |
| Семь сообщений `problems` интерполируют сырое имя параметра — суррогат доезжает до человека | internal | **Исправлено.** Единый `quoted(name)` через `JSON.stringify` во всех точках, плюс трейс на канонизируемость всего текста `problems` |
| Причина отказа молча режется на 1024 и схлопывает пробелы: S4 показывает залу другой, более короткий путь; законное имя `/root/a  b\tc.log` приезжает как несуществующее | internal, scan, error-obs | **Исправлено частично.** Заменить санитайзер нельзя — его требует R27. Потеря перестала быть молчаливой: изменённый текст помечается, имя, съеденное целиком, получает плейсхолдер вместо `''`. Остаток записан в ограничениях спеки |
| Двусторонняя перепись покрывает коды, а не ветки: две ветки `paths.ts` не производятся ни одним вектором | internal | **Исправлено частично.** Ветка «корень не резолвится» закрыта отдельным трейсом; ветка гейта R28 над результатом `realpath` **не фальсифицируема на macOS** (ядро отвергает имя с невалидным UTF-8, `EILSEQ`) — записано в ограничениях, а не засчитано покрытием |
| `switch (param.type)` в позиции оператора: шестая ветка `Param` приедет из схемы молча | scan | **Исправлено.** `default` с `const unreachable: never = param` |
| Слот `{}` в `argv` boolean-параметра не отвергается ничем и уезжает в командную строку литералом | scan | **Исправлено.** Отдельная проверка в ветке `boolean` + шестой вектор в таблице инвариантов |
| Голый `catch {}` на резолве корня: `ENOENT`, `ELOOP`, `EACCES`, `ENOTDIR` дают один текст — подмена корня в следе неотличима от опечатки в конфиге | error-obs | **Исправлено.** Ошибка связывается, код и сам корень едут в причину |
| «резолвнутый путь X лежит вне root: X» при `file: '.'` — сообщение врёт | internal, scan | **Исправлено.** Предикат даёт три исхода, у `root-itself` своя причина |
| Комментарий про порядок гейтов обоснован наоборот: «дешёвая» проверка стоила вдвое дороже той, от которой защищала | scan | **Исправлено.** Переписан по факту вместе с самой проверкой |
| Комментарий к `scrub` не называет схлопывание, `trim` и потолок 1024 | scan, error-obs | **Исправлено** |
| Предикат confinement скопирован в три места с расхождением; константа слота `{}` объявлена трижды | scan | **Исправлено внутри `core`** (слот — один экспорт, предикат — одна функция). Свести с `contracts` нельзя, не открыв `@mcpproxy/contracts/validate` белому списку R1: он тянет `re2`, `ajv`, `yaml`. Записано в ограничениях |
| Коммит `693c8db` озаглавлен `E0:`, лежит на ветке E2 и несёт посторонний `run.json`; правка `packages/contracts` затрагивает пять зависимых веток, ревизии их в дифе не видно | scan | **Отдано владельцу.** Переписывать историю под открытие PR не стал; названо в описании PR отдельной строкой вместе со списком зависимых веток |
| Расписка `build-test` на два коммита старше HEAD, `status.md` называет третий `codeTree` | scan | **Исправлено.** Гейт перепрогнан, `status.md` перерендерен |
| Порядок усечения отдаёт приоритет вытеснения атакующему: 32 мусорных ключа выбивают из записи единственный отказ с полезной нагрузкой | error-obs | **Исправлено.** Отказы по объявленным параметрам собираются первыми; в причине `denials-truncated` — три числа и разбивка по коду |
| Корневой вход `@mcpproxy/core` стал node-only | bc | **Отдано владельцу как факт, а не риск.** Пакет по ADR-0001 живёт на стороне демона и Electron не импортирует; потребителей у него сегодня нет — оба воркспейса-заглушки `export {}` |

## Nit

| Находка | Источник | Исход |
|---|---|---|
| `buildArgv` бросает там, где у стадии нет формы отказа; исключение уносит `timings` целиком | internal, error-obs | **Исправлено.** `PreparedRecipe` брендирован `unique symbol` и чеканится только в `prepareRecipe`: «закрыто на подготовке» стало гарантией компилятора |
| Срезалка комментариев в сорс-скане не знает о строковых литералах | internal | **Исправлено.** Третье утверждение в позитивный контроль фиксирует границу инструмента, а не притворяется, что её нет |
| Усечённый список даёт `DENIALS_MAX + 1` элемент | scan | **Исправлено документацией.** Маркер намеренно не входит в потолок: иначе факт усечения вытеснял бы строку, ради которой потолок существует |
| Спека R9 расходится с кодом по порядку проверок | internal | **Исправлено.** Устарела формулировка, а не код: предложение переписано, следствие (`too-long` вместо `pattern-mismatch`) названо там же |
| Предпосылка `permissive` — «RE2 пропускает суррогат на `^.{0,64}$`» — нигде не закреплена | test-quality | **Исправлено.** Три строки в `contracts/validate/regex.test.ts`, где движок настоящий; `core` остаётся без зависимости от `re2` |

## Опровергнуто — командой, а не мнением

| Находка | Источник | Чем опровергнута |
|---|---|---|
| `min: .nan` из YAML проходит схему и делает границы вечно-ложными | internal | `parseManifest(… min: .nan …)` → `ok: false`, диагностики `['schema: must be number', 'schema: must be number']`. Ajv отвергает NaN |
| `BigInt(1e308)` даёт элемент argv, «не ограниченный ничем» | scan | Ограничен по построению: наибольшее конечное двойное — 309 знаков, и это потолок, а не «ничем». Утечки нет, вектора нет |
| Целочисленное деление в `durationUs`: субмикросекундная стадия пишется как `0`, ноль неотличим от «не мерили» | scan | Опровергнуто вторым измерением: `event.ts:83` требует, чтобы `durationUs` был целым, а потеря — меньше микросекунды при бюджете 50 мс p95, то есть ниже разрешения самого измерения. «Не мерили» отличается наличием стадии в `timings`, а не величиной. Позитивный контроль на ненулевую длительность при этом добавлен |
| `paths.test.ts:163` тавтологичен (`toContain(realpathSync(lexRoot))` на том же входе) | test-quality (сам же и снял) | Несущая половина утверждения — `not.toContain('missing.log')`, и она проверяет границу R25/R26 |

## Что осталось владельцу

- [ ] OUTSTANDING — релиз-ноты: `bounds-satisfiable` ломает загрузку манифеста, отказ всеманифестный
- [ ] OUTSTANDING — ревизия веток e1/e3/e6/e7: эта ветка правит `packages/contracts`
- [ ] OUTSTANDING — `cwd` рецептом не ограничен: заводить ли инвариант в контракт
- [ ] OUTSTANDING — дрейф после слияния: main принёс 48 файлов E1/E6, ревьюеры E2 их не читали
- [ ] OUTSTANDING — дрейф: 9 прочитанных файлов правлены после ревью, нужен ли третий проход
- [ ] OUTSTANDING — коммит `693c8db`: заголовок `E0:` на ветке E2 + посторонний `run.json`

Развёрнуто по каждому — в конце этого файла и в `.review/`.

---

## Круг дельты — что нашло чтение правок

| Находка | Линза | Исход |
|---|---|---|
| **Регрессия R30а.** Обрыв на потолке стоял только на цикле неизвестных ключей; отказы по объявленным параметрам в потолок не упирались вовсе — 40 отказов при `DENIALS_MAX = 32` и **без** маркера усечения. Прежний `slice` резал весь список | internal | **Исправлено.** Общая функция `record` держит потолок для всего списка и считает остаток. Мутация красит новый трейс на 40 объявленных параметрах |
| **Регрессия бренда.** Двойной каст `as unknown as PreparedRecipe` снял структурную проверку литерала: `cwd: 42`, пропуск `exec` и подмена поля полем компилировались молча — раньше их держал возвращаемый тип | internal | **Исправлено.** `satisfies Omit<PreparedRecipe, typeof prepared>` до каста. Проверено в обе стороны: с `satisfies` `cwd: 42` даёт `TS2322`, без — компилируется молча |
| **Регрессия `default: never`.** Гейт компиляции не заменяет отказа: параметр с чужим `type` исчезал молча, `ok: true`, а запрос с ним получал `unknown-param` — след утверждал, что параметр не объявлен, хотя он объявлен | internal, error-obs | **Исправлено.** Ветка пишет `problems`, как и семь остальных инвариантов этой функции |
| **Сторож `include` не читает `exclude`.** Одна строка `exclude: [...]` при неизменном `include` уносит `deps.test.ts` и `harness.test.ts`: 104 теста и код возврата **0** — ровно та молчаливая потеря, против которой сторож написан | test-quality | **Исправлено.** Читаются оба списка; проверено — падение с поимённым перечислением |
| **`quoted()` не фальсифицируем.** Замена на identity оставляла 110/110 зелёными: на текст `problems` не смотрел ни один трейс | test-quality, error-obs | **Исправлено.** Трейс утверждает свойство, а не формулировку: весь текст `problems` обязан переживать канонизацию |
| **Пометка зачистки сняла инвариант длины.** `clean + MARK` приписывался поверх потолка: причина из 2000 символов приезжала длиной 1050, тогда как `scrub` гарантировал `<= 1024` | scan | **Исправлено.** Пометка приписывается внутри потолка |
| **Плейсхолдер врёт на пустом ключе.** `{"": 1}` — валидный JSON, зачистка в нём ничего не вырезала, а отказ говорил «имя вырезано зачисткой» | error-obs | **Исправлено.** Два разных текста; трейс на различимость |
| **Потеря в `paramName` осталась молчаливой**, в отличие от `reason`: ключ из 4096 эмодзи и ключ из 32 млн единиц приезжают одинаковыми 1024 точками | error-obs | **Исправлено.** Имя помечается по тому же правилу |
| **Ложное падение сторожа** на конфиге с двойными кавычками — валидный `include: ["…"]` ронял команду `test` | bc | **Исправлено.** Обе формы кавычек |
| **Довод в `regex.test.ts` слабее замеренного.** «Строгий паттерн отбивает» — довод *за* паттерн; настоящий механизм иной | scan | **Исправлено, и довод усилен по собственному замеру:** `new RE2('^.{0,64}$','u').exec('ab\uD800c')` → `['ab\uFFFDc']`. Движок подменяет непарный суррогат на U+FFFD и решает про строку, которой на входе не было, — то есть паттерн не может отбить такое значение **в принципе**, независимо от строгости автора |
| Число «87 тестов» устарело в двух местах | scan | **Исправлено** на замеренное: 7 файлов, 99 тестов |
| Перевод глоба продублирован в скрипте и в тесте без объяснения | scan | **Исправлено удалением.** Тест был строго слабее скрипта — на его месте комментарий, почему сторож не может жить внутри прогона |
| Затенение `label` параметром `label` в `requireCanonicalizable` | scan | **Исправлено.** Переименовано в `quoted` |
| «Показаны первые M» неверно относительно порядка вызывающего | error-obs | **Исправлено.** «Показано M» |
| Жёсткий разделитель `/` в новом скрипте повышает цену Windows-ограничения | scan | **Записано комментарием.** Спека Windows не обещает; строка стоит там, где живёт цена |
| Тест в замороженном `contracts` адресован эпику E2 | bc | **Исправлено.** Переписан так, чтобы стоять сам по себе: он про поведение движка, а не про чужой инвариант |
| Ресурсные трейсы — флейк? | test-quality | **Опровергнуто замером.** Худший запас под нагрузкой (16 busy-loop на 14 ядрах) — **64x**: 0.39 мс против порога 25 мс, 12/12 зелёных прогонов подряд. Дефект под теми же порогами даёт 119–164 мс. Замена без часов (дочерний процесс с `--max-old-space-size=96`: rc 0 против rc 134) проверена и готова, если запас станет тесен. Альтернатива «утверждать дельту `heapUsed`» проверена и отвергнута: пять замеров дефекта дали 274 / −244 / 244 / −0.0 / −0.0 МБ |

Отдельно из дельты, не находка: `codePointLengthAtMost` эквивалентен точному счёту не по
доводу, а по **перебору** — алфавит `a` · `U+1F600` · одиночный high · одиночный low, все
строки длины 0…6, каждый `max` от 0 до 8: **49 149 пар, 0 расхождений**.
