// E1 policy · E2 validate · E3 exec · E6 audit. Без импортов Electron — см. ADR-0001.
//
// Экспорт — ОДИН корневой barrel (R35). Subpath-входы не заводятся: они понадобятся, когда
// E3 принесёт node-only зависимости, и заводить их заранее значит замораживать форму, у
// которой ещё нет потребителя. Границу графа держит `deps.test.ts`, а не этот комментарий.
export * from './validate/index.js';
