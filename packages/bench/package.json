{
  "name": "@mcpproxy/bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit false --emitDeclarationOnly",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "tsc -b && vitest run",
    "bench": "node bin/mcpproxy-bench.mjs"
  },
  "dependencies": {
    "@mcpproxy/contracts": "workspace:*",
    "@mcpproxy/core": "workspace:*",
    "@mcpproxy/mcp-server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "vitest": "^3"
  },
  "bin": {
    "mcpproxy-bench": "./bin/mcpproxy-bench.mjs"
  }
}
